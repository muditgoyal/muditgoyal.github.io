[project]
name = "sf-events"
version = "0.1.0"
description = "Acontece — SF events aggregator"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
    "feedparser>=6.0",
    "lxml>=5.0",
]

[tool.ruff]
line-length = 120
